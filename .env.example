# Apollo Cold Emailer - Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# REQUIRED - Apollo API
# =============================================================================
# Get your API key from: https://app.apollo.io/#/settings/integrations/api
# NOTE: Requires a PAID Apollo plan ($49+/month) - free plans have no API access
APOLLO_API_KEY=your_apollo_api_key_here

# =============================================================================
# OPTIONAL - AI Email Generation
# =============================================================================
# Provider: mock (no API, placeholder text), gemini (Google), openai (OpenAI)
LLM_PROVIDER=mock

# Google Gemini API Key (recommended - free tier available)
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI API Key (alternative - costs money)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Model to use (for Gemini: gemini-2.5-flash, for OpenAI: gpt-4, gpt-3.5-turbo)
LLM_MODEL=gemini-2.5-flash

# =============================================================================
# OPTIONAL - Email Sending
# =============================================================================
# Provider: mock (console only), smtp (actually send emails)
EMAIL_PROVIDER=mock

# SMTP Server Configuration (for Gmail, Outlook, etc.)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Your email address
SMTP_EMAIL=your_email@gmail.com

# For Gmail: Use App Password (not your regular password)
# Enable 2FA first: https://myaccount.google.com/security
# Create App Password: https://myaccount.google.com/apppasswords
SMTP_PASSWORD=your_app_password_here

# Directory containing your resume (any PDF in this folder will be used)
RESUME_DIR=docs

# =============================================================================
# DATABASE - PostgreSQL Configuration
# =============================================================================
# Database connection URL
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/apollo_contacts

# Alternative: Individual connection parameters
DB_HOST=localhost
DB_PORT=5432
DB_NAME=apollo_contacts
DB_USER=postgres
DB_PASSWORD=your_db_password_here

# =============================================================================
# OPTIONAL - Other Settings
# =============================================================================
# Directory for saving contact JSON exports (backup)
DEFAULT_OUTPUT_DIR=outputs

# Default number of results per API page
DEFAULT_PER_PAGE=100
